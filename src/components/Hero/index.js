import React from 'react';
import {} from './Styled';

const HeroBuild = () => {
  return <></>;
};

HeroBuild.defaultProps = {};

const Hero = React.memo(HeroBuild);

export default Hero;
export { Hero };
